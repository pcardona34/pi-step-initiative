# Notifications

sudo apt install dunst

## Add to autostart:

systemctl --user import-environment DISPLAY
/usr/bin/dunst &

## config in:
$HOME/.config/dunst/dunstrc

See also the TOOLS under 'pisin/TOOLS' folder, namely:

- Updater
- BirthNotify
